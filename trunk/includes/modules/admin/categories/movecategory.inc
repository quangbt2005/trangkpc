<?php
/**************************************************************************************************/
/* Created on Dec 4, 2008                                                                         */
/**************************************************************************************************/
require_once( FUNCTIONS_PATH . "DB.inc" );
// ---------------------------------------------------------------------------------------------- //
//
// ---------------------------------------------------------------------------------------------- //
if(! Check_Admin_Ajax()) exit;

$cat_id = $Request['parameters'][':catid'];
$des_id = $Request['parameters'][':desid'];

if(!empty($cat_id) && !empty($des_id)){
  DB_Update('categories', array('categories_id' => $cat_id), array('parent_id' => $des_id));
}
?>
